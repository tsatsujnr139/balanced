/** No-op on non-iOS platforms — home screen widgets are iOS-only. */
export function refreshHomeScreenWidgets() {
  // Intentionally empty: Android/web have no expo-widgets home screen support yet.
}
